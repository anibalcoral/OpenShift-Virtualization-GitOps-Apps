apiVersion: v1
kind: Secret
metadata:
  name: workshop-ssh-key
type: Opaque
stringData:
  key: |
    # This SSH key will be automatically generated and populated by the setup-ssh-key.yaml playbook
    # Run: ansible-playbook playbooks/setup-ssh-key.yaml to generate and populate this secret
    # DO NOT commit real SSH keys to this repository