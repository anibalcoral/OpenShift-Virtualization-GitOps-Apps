apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: workshop-gitops-vms-hml

namePrefix: hml-

patches:
  - patch: |-
      - op: replace
        path: /spec/template/spec/domain/resources/requests/memory
        value: 2Gi
      - op: replace  
        path: /spec/template/spec/domain/cpu/sockets
        value: 1
    target:
      kind: VirtualMachine
  - patch: |-
      - op: replace
        path: /spec/to/name
        value: hml-vm-web-service
      - op: add
        path: /spec/host
        value: hml-workshop-gitops-vms.apps.cluster-st22j.dynamic.redhatworkshops.io
    target:
      kind: Route
      name: vm-web-route