apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: workshop-gitops-vms-prd

namePrefix: prd-

patches:
  - patch: |-
      - op: replace
        path: /spec/template/spec/domain/resources/requests/memory
        value: 4Gi
      - op: replace
        path: /spec/template/spec/domain/cpu/sockets  
        value: 2
    target:
      kind: VirtualMachine
  - patch: |-
      - op: replace
        path: /spec/to/name
        value: prd-vm-web-service
      - op: add
        path: /spec/host
        value: workshop-gitops-vms.apps.cluster-st22j.dynamic.redhatworkshops.io
    target:
      kind: Route
      name: vm-web-route